{
  "name": "melody-frontend",
  "private": true,
  "version": "4.1.0",
  "description": "Melody 前端（esbuild 打包为单文件嵌入 exe）",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "test": "node scripts/test-bundle.mjs && node scripts/test-services.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "jsdom": "^30.0.1"
  }
}